import React from 'react';

const AssignmentModal = () => {
  return (
    <button onClick={() => alert('Assignment Modal Opened!')}>Add Assignment</button>
  );
};

export default AssignmentModal;
